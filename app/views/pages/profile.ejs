<section class="hero">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-mobile is-centered">
                <div class="column is-half">
                    <h1 class="title">Số dư</h1>
                    <table class="table is-fullwidth">
                        <thead>
                            <tr>
                                <th>Tiền</th>
                                <th>Địa chỉ ví</th>
                                <th>Tổng số dư</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (var account of accounts) { %>
                            <tr>
                                <td><%= account.asset %></td>
                                <td><%= account.address %></td>
                                <td><%= account.balance %></td>
                            </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
<section class="section">
    <div class="container"></div>
</section>
<section class="hero">
    <div class="hero-body">
        <div class="container"></div>
    </div>
</section>
